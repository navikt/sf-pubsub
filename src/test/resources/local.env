# Replace this with content of teamcrm secret local.env for running demo
SF_TOKENHOST=
SF_USERNAME=
SF_CLIENT_ID=
PRIVATE_KEY_ALIAS=
PRIVATE_KEY_PASSWORD=
KEYSTORE_PASSWORD=
KEYSTORE_JKS_B64=

# To create events for demo login to https://workbench.developerforce.com/execute.php
# with credential of teamcrm secret sf-pubsub-wb-user

# Run for instance:

#// Create an instance of the BjornMessage__e platform event
#BjornMessage__e event = new BjornMessage__e();
#
#// Set the value for the Message__c field
#event.Message__c = 'Good morning';
#
#// Publish the event
#Database.SaveResult result = EventBus.publish(event);
#
#// Check if the event was successfully published
#if (result.isSuccess()) {
#    System.debug('Event published successfully!');
#} else {
#    System.debug('Failed to publish event: ' + result.getErrors()[0].getMessage());
#}