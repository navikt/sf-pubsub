apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: user-updates
  namespace: ips
  labels:
    team: ips
  annotations:
    kafka.nais.io/removeDataWhenResourceIsDeleted: "true"
spec:
  pool: nav-dev
  config:
    cleanupPolicy: delete
    minimumInSyncReplicas: 1
    partitions: 1
    replication: 3
    retentionBytes: -1
    retentionHours: 720 #30 days
  acl:
    - team: ips
      application: sf-pubsub-user-updates
      access: readwrite
    - team: teamoppfolging
      application: teamoppfolging-kafka
      access: read
