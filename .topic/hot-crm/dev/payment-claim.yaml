apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: payment-claim
  namespace: hot-crm
  labels:
    team: hot-crm
  annotations:
    kafka.nais.io/removeDataWhenResourceIsDeleted: "true"
spec:
  pool: nav-dev
  config:
    cleanupPolicy: delete
    minimumInSyncReplicas: 1
    partitions: 1
    replication: 3
    retentionBytes: -1
    retentionHours: 720 #30 days
  acl:
    - team: hot-crm
      application: sf-pubsub-payment-claim
      access: readwrite
    - team: hot-crm
      application: sf-payment-claim
      access: read
    - team: oksty # <- Update when team exist
      application: tolketjenester # <- Update when app exist
      access: read