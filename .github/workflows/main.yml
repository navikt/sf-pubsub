name: Preconfigured run on push

on:
  push:
    branches-ignore:
      - 'chore/**'
    paths-ignore:
      - README.md
      - .topic/**
      - .github/workflows/topics.yml

jobs:
  build-deploy:
    uses: ./.github/workflows/workflow.yml
    permissions:
      id-token: write
      contents: write
    secrets: inherit
    with:
      DEPLOY_APP: sf-pubsub-user-updated
      DEPLOY_CLUSTER: dev-gcp
      DEPLOY_NAMESPACE: jobbsporet # TODO replace with folder structure or file scanning in future to support deploy ALL
